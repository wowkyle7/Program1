#include "PetClass.h"

// Default 
Pet::Pet()
{
    species;
    name = ""; 
    sex = ''; 
    age = 0; 
    price = 0.00; 
}

// Overloaded 
Pet::Pet(string, string, char, int, double)
{
    species = s; 
    name = n;
    sex = s; 
    age = a; 
    price = p; 
}

